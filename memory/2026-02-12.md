# 2026-02-12 - 配置恢复与KimiCode设置

## 重要事件

### 1. KimiCode模型配置
- 添加了新的模型配置 `kimi-coding/k2.5`
- 使用独立的API Key: `sk-kimi-Fdk8tAo5R2ZKhHFyOXNvWfLhN2qx7MljPwsOScwSPTDnfQhmQgbb4Y1lg1vS3Ija`
- 默认模型保持为 `moonshot/kimi-k2.5` 不变

### 2. 配置文件恢复教训 ⚠️
**严重错误**：在编辑配置文件时破坏了JSON结构，导致刚哥不得不重新初始化配置。

**正确做法**：
- 不要完全替换配置文件
- 使用精确的 `edit` 操作修改特定字段
- 修改前备份原文件
- 修改后验证JSON格式

### 3. 关键配置修复
为所有provider添加了 `authProfile` 引用：
```json
"moonshot": {
  "authProfile": "moonshot:default",
  ...
}
"kimi-coding": {
  "authProfile": "kimi-coding:default",
  ...
}
"openrouter": {
  "authProfile": "openrouter:default",
  ...
}
```

### 4. 当前可用模型
- Kimi (默认)
- KimiCode
- Claude
- GPT4o
- GPT4o-mini
- DeepSeek

## 配置备份
原始配置备份：`~/openclaw/openclaw_old.json`

## 教训
编辑配置文件时要格外小心，避免破坏原有结构。优先使用精确编辑而非完全替换。
